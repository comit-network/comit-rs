table! {
    swaps {
        id -> Integer,
        swap_id -> Text,
        alpha_ledger -> Integer,
        beta_ledger -> Integer,
        alpha_asset -> Integer,
        beta_asset -> Integer,
        role -> Integer,
    }
}
