table! {
    swaps {
        id -> Integer,
        swap_id -> Text,
        alpha_ledger -> Text,
        beta_ledger -> Text,
        alpha_asset -> Text,
        beta_asset -> Text,
        role -> Text,
    }
}
